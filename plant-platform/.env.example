DATABASE_URL=postgres://user:password@host/database?sslmode=require

# Vercel Postgres/Neon integrations may provide POSTGRES_URL instead.
# The app also accepts POSTGRES_URL, POSTGRES_PRISMA_URL, or POSTGRES_URL_NON_POOLING.
